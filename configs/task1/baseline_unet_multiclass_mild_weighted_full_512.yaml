name: task1_baseline_unet_multiclass_mild_weighted_full_512
seed: 20260520

paths:
  repo_root: .
  train_manifest: configs/task1/splits/released_train.csv
  eval_manifest: configs/task1/splits/released_eval.csv
  output_dir: outputs/task1/baseline_unet_multiclass_mild_weighted_full_512

data:
  image_size: [512, 512]
  label_mode: multiclass_012
  threshold: 0.5
  max_train_samples: null
  max_eval_samples: 512
  max_predict_samples: 64

model:
  name: tiny_unet
  in_channels: 3
  out_channels: 3
  base_channels: 16

loss:
  name: weighted_cross_entropy
  class_weights: [0.02, 1.0, 2.0]

training:
  device: cuda
  epochs: 15
  batch_size: 64
  num_workers: 8
  learning_rate: 0.001
  weight_decay: 0.0001
  log_interval: 25
  monitor_metric: eval.dice
  monitor_mode: max
